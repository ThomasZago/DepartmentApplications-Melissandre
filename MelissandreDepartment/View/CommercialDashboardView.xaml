<Page x:Class="MelissandreDepartment.View.CommercialDashboardView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:MelissandreDepartment.View"
      xmlns:vm="clr-namespace:MelissandreDepartment.ViewModel"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="CommercialDashboardViewModel"
      DataContext="{Binding Source={x:Static vm:CommercialDashboardViewModel.Instance}}">

    <Grid>
        <ProgressBar Style="{StaticResource MaterialDesignCircularProgressBar}" Value="{Binding AcceptedPercentage}"/>
        <ProgressBar Style="{StaticResource MaterialDesignCircularProgressBar}" Value="{Binding AcceptedPercentage}"/>
        <ProgressBar Style="{StaticResource MaterialDesignCircularProgressBar}" Value="{Binding AcceptedPercentage}"/>
    </Grid>
</Page>
